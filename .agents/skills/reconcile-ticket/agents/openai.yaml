interface:
  display_name: "Reconcile Ticket"
  short_description: "Accept, return, or design follow-up work"
  default_prompt: "Use $reconcile-ticket to decide the outcome of this verified Mile42 PR."
policy:
  allow_implicit_invocation: true
