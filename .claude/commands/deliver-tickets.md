---
description: Plan and orchestrate related or independent Mile42 tickets through implementation, verification, and reconciliation.
argument-hint: <space-delimited issue numbers>
allowed-tools: Bash, Read, Glob, Grep, Edit, Write, Agent, TaskCreate, TaskGet, TaskOutput, TaskList, TaskStop, Skill
---

Read `.agents/skills/deliver-tickets/SKILL.md` completely, including every
reference it requires, then follow that canonical skill for **$ARGUMENTS**.

This file is only the Claude command adapter. If it conflicts with the
canonical skill, the canonical skill wins.
