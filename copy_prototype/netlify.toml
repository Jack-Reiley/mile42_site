[build]
  base = "copy_prototype"
  command = "npm run build"
  publish = "dist"

# Prototype only. Must not be indexed.
[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
